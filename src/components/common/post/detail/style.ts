import styled from "styled-components";

export const SubContentsWrap = styled.div`
  width: 50%;
  margin: 3rem auto;
`;

export const PostTitleContext = styled.p`
  font-size: 1.25rem;
  font-weight: 600;
`;

export const PostDateContext = styled.p`
  font-size: 0.75rem;
  font-weight: 600;
  color: #aaa;
`;
